import React, { ReactNode, ReactElement } from "react";

interface LayoutProps {
  children: ReactNode;
}

export default function MainLayout({ children }: LayoutProps): ReactElement {
  return <div></div>;
}
